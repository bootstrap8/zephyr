# LLM 对话接入 — 待办清单

## 子问题分解

| # | 子问题 | 说明 | 状态 |
|---|--------|------|------|
| 1 | 会话/消息持久化 | conversations + messages 表，CRUD 替换 mock | ✅ 完成 |
| 2 | MCP 工具运行时调用 | McpClient 增加 tools/call，支持 tool calling 循环 | ✅ 完成 |
| 3 | 上下文组装 + 模型调用 | 上下文 + tool 分发 + OkHttp SSE 流式 | ✅ 完成 |
| 4 | Skill 集成 | 已并入 #3，通过 use_skill tool 懒加载 | ✅ 完成 |
| 5 | `/` 命令输入 | 前端输入框 `/` 触发命令菜单 | ✅ 完成 |
| 6 | 模型调用 & SSE 流式 | 已并入 #3，OkHttp + SSE 解析 | ✅ 完成 |

## 依赖关系

```
1 (持久化) ──┐
             ├──→ 3 (上下文组装) ──→ 6 (模型调用 & SSE)
2 (工具调用) ─┘
4 (Skill) ────┘
5 (/) ──────── 前端独立，可并行
```

## 建议顺序

1 → 2 → 3 → 6 → 4 → 5

先做基础设施（持久化、工具调用），再组装上下文，最后接入真实模型流式。
