<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.github.hbq969.ai.zephyr.builtintool.dao.BuiltinToolDao">

  <update id="createBuiltinToolControlsTable">
    create table if not exists zephyr_builtin_tool_controls (
      tool_name      varchar(64)   primary key,
      description    varchar(512)  default '',
      require_admin  int           default 1,
      created_at     bigint,
      updated_at     bigint
    )
  </update>

</mapper>
